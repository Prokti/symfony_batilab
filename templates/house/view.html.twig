{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>New House</h1>
                <div>

                {{ form_start(houseForm) }}
                {{ form_widget(houseForm) }}
                <button type="submit" class="btn btn-primary">Save</button>
                {{ form_end(houseForm) }}
            </div>
            </div>
        </div>
    </div>

{% endblock %}